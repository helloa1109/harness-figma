import { Button } from "./components/ui/Button";

export function App() {
  return (
    <main style={{ padding: "var(--space-xl)" }}>
      <h1>Travel App</h1>
      <p>하네스 템플릿 동작 확인용 진입점.</p>
      <Button>시작하기</Button>
    </main>
  );
}
